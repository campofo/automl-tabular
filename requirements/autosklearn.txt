# auto-sklearn is Linux-only (use Docker or WSL2 elsewhere) and needs
# Python <=3.9 for prebuilt wheels of its scikit-learn<0.25 pin. Installing
# this file downgrades scikit-learn/numpy below the base pins, which is
# expected - keep auto-sklearn in its own environment/image.
auto-sklearn==0.15.0
# numpy 1.24 removed the deprecated aliases this old stack still uses
numpy<1.24
